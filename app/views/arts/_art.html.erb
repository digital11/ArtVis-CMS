<tr>
  <td><%= art.title %></td>
  <td><%= image_tag art.image.url(:mini) %></td>
  <td><%= link_to 'Edit', edit_art_path(art), class: 'btn btn-primary btn-mini' %></td>
  <td><%= link_to("Delete", art, :data => { :confirm => "Are you sure?" }, :method => :delete, :class => 'btn btn-mini')  %></td>
</tr>